# 开发状态

进行中的工作按主线隔离，避免设计、验收和待办事项在并行开发时互相覆盖。

- `paper/`：论文线的进行中数据、模型与实验变更；
- `career/`：求职线的进行中展示项目变更；
- `shared/`：同时影响两条线的共享底座变更。

新工作只在所属目录中创建状态文档。`current-change.md` 是此前单文档流程的历史记录，不再追加新内容。长期有效的项目说明仍分别留在 `docs/paper/`、`docs/career/` 和架构文档中。
